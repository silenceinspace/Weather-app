import { inputController, keepYearInFooterUpdated } from './dom-manipulation';
import './styles.css';

// Attach event listeners to enable the app's functionality + decide how the initial view of the page is going to look
inputController();
keepYearInFooterUpdated();
